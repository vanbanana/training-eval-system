- **字体配置**：运行 `make setup-fonts` 下载 Noto Sans SC 字体以支持 PDF 报告中的中文渲染。
- **环境变量**：必须设置 `TES_JWT_SECRET` (≥32字符) 和 `TES_LLM_KEY_MASTER` (Base64编码的32字节密钥)。
- **构建命令**：使用 `make build` 进行原生构建，或 `make cross-compile` 交叉编译为 LoongArch64 (龙芯) 静态二进制文件。