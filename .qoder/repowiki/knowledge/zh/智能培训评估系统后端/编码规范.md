- 所有业务错误统一封装在 `internal/apperr` 包中，并通过 `handler` 层转换为标准的 JSON 错误响应。
- 数据访问层遵循 Repository 模式，每个实体对应一个 Repo 接口及其 SQLite 实现，通过 `internal/store` 提供的读写连接执行 SQL。
- 耗时操作（如文档解析、LLM 调用）均提交至 `worker.Pool` 异步执行，避免阻塞 HTTP 请求 goroutine。
- 配置加载统一通过 `internal/config` 包从 `TES_` 前缀的环境变量或 `.env` 文件中读取，并在启动时进行严格校验。