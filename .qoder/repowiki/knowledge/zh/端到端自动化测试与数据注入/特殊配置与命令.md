- **启动脚本**：执行 `powershell -ExecutionPolicy Bypass -File run_e2e.ps1` 可一键完成环境检查、服务启动及全量测试。
- **环境依赖**：需预先配置 `MIMO_API_KEY` 环境变量或 `.env.e2e` 文件以支持 LLM 评分链路测试。
- **数据准备**：运行 `seed_real_data.py` 可从指定数据集目录解析学生信息并批量注入课程、班级及任务数据。