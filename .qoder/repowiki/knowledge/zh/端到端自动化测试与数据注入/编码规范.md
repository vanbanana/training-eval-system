- 所有测试脚本均定义全局 `BASE_URL` 配置，优先读取 `TES_E2E_URL` 或 `TES_BASE_URL` 环境变量，默认指向 `http://localhost:8000`。
- API 客户端普遍采用封装模式，在 `login` 方法中自动提取 `access_token` 并注入到后续请求的 `Authorization: Bearer` 头部。
- 关键业务流程（如上传、评分触发）均包含显式的等待或轮询逻辑，通过 `time.sleep` 或状态码检查确保异步流水线执行完毕。